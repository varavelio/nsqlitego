version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - govet
    - errcheck
    - ineffassign
    - staticcheck
    - unused
    - bodyclose
    - godoclint
    - godot
    - errorlint
    - nilerr
    - nilnesserr
    - durationcheck
    - makezero
    - noctx
  exclusions:
    paths:
      - internal/vdl

formatters:
  enable:
    - gofumpt
    - goimports
    - golines
  settings:
    gofumpt:
      extra-rules: true
  exclusions:
    paths:
      - internal/vdl
